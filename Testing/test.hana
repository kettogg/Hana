--fn inc(var x)
--	return x+1

--var y = inc(1)
--printvalue(y)

--double pi_inc = inc(3.14)
--printdouble(pi_inc)
--int one_inc_by_two = inc(2)
--printvalue(one_inc_by_two)

fn myFun( let x, let y )
    return x+y

let res = myFun( 1, 2 )

displayln("%d", res)